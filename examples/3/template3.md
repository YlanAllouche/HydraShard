<%*
  let menuitems = [
    {
      key: "f",
      name: "Do something",
      action: async () => {
// Write anything here and define everything into asingle template
      },
    },]
%>

<%*
let result = tp.user.hydra(menuitems)
%>
